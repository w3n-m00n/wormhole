{
  "name": "wormhole",
  "version": "1.0.0",
  "scripts": {
    "start": "serve",
    "build": "sass scss/style.scss css/style.css",
    "dev": "serve & nodemon --exec 'yarn build'"
  },
  "private": true,
  "devDependencies": {
    "nodemon": "^2.0.6",
    "sass": "^1.29.0"
  }
}
